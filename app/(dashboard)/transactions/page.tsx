import { PagePlaceholder } from "@/components/page-placeholder";

export default function TransactionsPage() {
  return (
    <PagePlaceholder
      title="Transactions"
      description="Reconcile payouts, review refunds, and keep an eye on high-value orders. Integrate with your payment provider to populate live settlement data."
      cta="Sync payments"
    />
  );
}

