import { PagePlaceholder } from "@/components/page-placeholder";

export default function FeedbackPage() {
  return (
    <PagePlaceholder
      title="Feedback Center"
      description="Collect ideas from teammates and customers, then prioritize improvements. Hook this up to your issue tracker to convert feedback into tasks."
      cta="Submit feedback"
    />
  );
}

